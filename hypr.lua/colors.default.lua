-- Fallback palette, used until wallust has run once.
--
-- The live palette is hypr.lua/colors.lua, written by wallust on every wallpaper change
-- and never tracked (it is machine-specific and churned on every commit). That left a
-- fresh install with no palette at all: hyprland.lua's own comment promised a shipped
-- default to fall back on, and there was none, so every window border and shadow came up
-- as Hyprland's own colours until the first wallpaper was picked.
--
-- Deliberately neutral: this is what a desktop looks like for the minute between the
-- first login and the first wallpaper, not a theme anybody chose.
foreground = "#D8D8D8"
background = "#111111"
color0     = "#111111"
color1     = "#3A3A3A"
color2     = "#4A4A4A"
color3     = "#5C5C5C"
color4     = "#6E6E6E"
color5     = "#8A8A8A"
color6     = "#A6A6A6"
color7     = "#D8D8D8"
color8     = "#606060"
color9     = "#3A3A3A"
color10    = "#4A4A4A"
color11    = "#5C5C5C"
color12    = "#6E6E6E"
color13    = "#8A8A8A"
color14    = "#A6A6A6"
color15    = "#F0F0F0"
