import ".."
import QtQuick

// Small-caps group header, e.g. "SYSTEM OSD".
Text {
    color:              Colors.fgMuted
    font.pixelSize:     Style.fsSection
    font.bold:          true
    font.letterSpacing: 0.5
    font.family:        Style.font
}
