# docs/

The documentation lives in its own site, not here: **[velumeron-wiki](https://github.com/vredix-openvuture/velumeron-wiki)**.

Installation, every setting, every module, the IPC surface, the compositor layer and the
troubleshooting pages are all there, and only there. Two documentation sets drift apart within a
month, so this folder holds no pages.

What is left here is `img/`: the screenshots the README embeds.
